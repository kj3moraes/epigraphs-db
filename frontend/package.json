{
  "name": "epigraphs-graph",
  "version": "1.0.0",
  "description": "3D interactive graph of literary epigraph connections",
  "scripts": {
    "start": "esbuild index.js --bundle --outfile=bundle.js --format=iife --platform=browser --minify",
    "dev": "esbuild index.js --bundle --outfile=bundle.js --format=iife --platform=browser --watch"
  },
  "dependencies": {
    "3d-force-graph": "^1.73.3",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
